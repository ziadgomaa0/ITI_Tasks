#include<stdio.h>

typedef sruct node 
{
    int val;
    int *next; // defining node
}

void push _end (node *current_head,int data)
{
    while (current head ->next !=NULL)
    {
        current_head = current_head ->
    }
}